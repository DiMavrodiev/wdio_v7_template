import { homepage } from '../../pageobjects'

describe('Homepage buttons verification', () => {

    before(() => {
        browser.url('/')
    });

    it('Should have Demo and How it works buttons',  () => {

    });

})